Created repository in the directory of the django project, so unfortunately pip file is 1 directory above and is not tracked. Will periodically copy it to _copy_of_pipfile directory. Lesson learned.
Will add git repository 1 dir above the django project, so PIP file will be versioned as well.
